services:
  - type: web
    name: socialcare-website
    runtime: node
    plan: free
    buildCommand: npm install --prefix client && npm run build --prefix client && npm install --prefix server && cp -r client/dist server/dist
    startCommand: node server/server.js
    healthCheckPath: /api/healthcheck